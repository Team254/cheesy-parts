<table  class="table table-striped table-bordered">
  <% statuses = @status.nil? ? Part::STATUS_MAP.keys.reject { |k| k == "done" } : [@status] %>
  <% statuses.each do |status| %>
    <% parts = Part.where(:project_id => @project.id, :status => status).order_append(:priority) %>
    <% next if parts.empty? && statuses.size > 1 %>
    <tr>
      <td style="padding: 10px 15px 20px 15px;">
        <h3 class="dashboard-status"><%= Part::STATUS_MAP[status] %>:</h3>
        <% parts.each do |part| %>
          <a href="/parts/<%= part.id %>" title="<%= part.name %>">
            <div class="dashboard-part priority-<%= part.priority %>">
              <%= part.full_part_number %>
            </div>
          </a>
        <% end %>
      </td>
    </tr>
  <% end %>
</table>
